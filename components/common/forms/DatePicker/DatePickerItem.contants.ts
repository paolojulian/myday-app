export const DATE_PICKER_ITEM_TEST_IDS = {
  containerBtn: (id: string) => `date-picker-item-container-${id}`,
  isActive: (id: string, isActive: boolean) => `date-picker-item-selected-${id}-${isActive}`,
};
